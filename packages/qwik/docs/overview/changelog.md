# Changelog

## 0.2.0 (08.11.2025)

### 🚀 Features

- **Introduce a `Theme` component.** Adds a comprehensive theming system, enabling configuration and management of the application's color scheme (light/dark/system), border radius, and UI scaling preferences.

- **Introduce a `Button` component.** Adds the main component for user interaction, featuring support for multiple sizes, variants, colors, and a dedicated mechanism for handling loading states to prevent user errors.

- **Introduce an `IconButton` component.** Adds a specialized button for triggering actions with a single icon, ensuring full style consistency with the main button and accessibility during the loading state.

- **Introduce a `Spinner` component.** Adds the composable component to display an animated loading indicator for a better user experience during data fetching or processing.

- **Introduce a `VisuallyHidden` component.** Adds a utility component for hiding content visually while keeping it accessible to screen readers, crucial for enhancing accessibility.

## 0.1.0 (24.09.2025)

- **Initial release.**
